# Machine-generated inputs and outputs. Large, re-fetchable, and none of them
# is a source of truth — the scripts beside them are.
*.json
*.m3u
dup_ids.txt
__pycache__/

# Crest source trees, re-fetched with the gh calls in crest_match.py.
crest_tree_*.txt

# Guide packs, re-fetched by epg_fill.py from the repo source.
*.gz
*.xml
